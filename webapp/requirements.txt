# Webapp-specific requirements (adds gradio + torch on top of the core
# package's deps). Used directly in place -- by render.yaml's buildCommand
# and by local dev (`pip install -r webapp/requirements.txt`). See
# webapp/DEPLOY.md for the current deploy situation.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
opencv-python-headless==5.0.0.93
mediapipe==0.10.35
pyiqa==0.1.15.post2
ImageHash==4.3.2
# unpinned: gradio 5.9.1 requires pillow<12.0, which conflicts with the
# core package's Pillow==12.3.0 pin -- let pip pick a compatible version.
Pillow<12.0
numpy==2.4.6
gradio==5.9.1
